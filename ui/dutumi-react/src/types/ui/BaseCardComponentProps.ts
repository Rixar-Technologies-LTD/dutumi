
interface BaseCardComponentProps {
    children : any,
    title: string,
    iconImage?: any,
    subTitle?: string,
    margin?: number,
    extraHeaderItems : [...any]
}

export default BaseCardComponentProps;
